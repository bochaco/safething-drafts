version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: safe-wallet_web_1
      APP_PORT: 3000

  web:
    image: getumbrel/safe-wallet:v0.0.16
    restart: on-failure
    stop_grace_period: 1m
    environment:
      SAFE_WALLET_PORT: 3000
